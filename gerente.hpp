#ifndef GERENTE_HPP
#define GERENTE_HPP

void addAltSanduiche(std::string nome, float preco);
void excluirSanduiche(std::string nome);
void salvarSanduiches();

void addAltSuco(std::string nome, float preco);
void excluirSuco(std::string nome);
void salvarSucos();

#endif